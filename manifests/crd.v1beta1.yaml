# DEPRECATED: This v1beta1 CRD file is deprecated and not supported for Kubernetes 1.22+
# 
# v1beta1 CRDs were removed in Kubernetes 1.22+. 
# For Kubernetes 1.22+, use the v1 CRDs instead:
#   - manifests/crd-combined.yaml (combined file)
#   - manifests/crd/*.yaml (individual CRD files)
#
# This file is kept only for reference/historical purposes.
# 
# The current tikv-operator v2 uses these CRDs:
# - clusters.core.tikv.org (Cluster)
# - pdgroups.core.tikv.org (PDGroup)
# - pds.core.tikv.org (PD)
# - tikvgroups.core.tikv.org (TiKVGroup)
# - tikvs.core.tikv.org (TiKV)
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: clusters.core.tikv.org
spec:
  group: core.tikv.org
  scope: Namespaced
  names:
    plural: clusters
    singular: cluster
    kind: Cluster
    listKind: ClusterList
  versions:
  - name: v1alpha1
    served: true
    storage: true
  validation:
    openAPIV3Schema:
      type: object
      properties:
        spec:
          type: object
          properties:
            paused:
              type: boolean
            revisionHistoryLimit:
              type: integer
        status:
          type: object
  additionalPrinterColumns:
  - JSONPath: .status.components[?(@.kind=="PD")].replicas
    name: PD
    type: integer
  - JSONPath: .status.components[?(@.kind=="TiKV")].replicas
    name: TiKV
    type: integer
  - JSONPath: .status.conditions[?(@.type=="Available")].status
    name: Available
    type: string
  - JSONPath: .metadata.creationTimestamp
    name: Age
    type: date
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: pdgroups.core.tikv.org
spec:
  group: core.tikv.org
  scope: Namespaced
  names:
    plural: pdgroups
    singular: pdgroup
    kind: PDGroup
    listKind: PDGroupList
    shortNames:
    - pdg
  versions:
  - name: v1alpha1
    served: true
    storage: true
  validation:
    openAPIV3Schema:
      type: object
  additionalPrinterColumns:
  - JSONPath: .spec.cluster.name
    name: Cluster
    type: string
  - JSONPath: .spec.replicas
    name: Desired
    type: integer
  - JSONPath: .status.readyReplicas
    name: Ready
    type: integer
  - JSONPath: .status.updatedReplicas
    name: Updated
    type: integer
  - JSONPath: .status.conditions[?(@.type=="Ready")].status
    name: Ready
    type: string
  - JSONPath: .metadata.creationTimestamp
    name: Age
    type: date
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: pds.core.tikv.org
spec:
  group: core.tikv.org
  scope: Namespaced
  names:
    plural: pds
    singular: pd
    kind: PD
    listKind: PDList
  versions:
  - name: v1alpha1
    served: true
    storage: true
  validation:
    openAPIV3Schema:
      type: object
  additionalPrinterColumns:
  - JSONPath: .spec.cluster.name
    name: Cluster
    type: string
  - JSONPath: .status.isLeader
    name: Leader
    type: string
  - JSONPath: .status.conditions[?(@.type=="Ready")].status
    name: Ready
    type: string
  - JSONPath: .metadata.creationTimestamp
    name: Age
    type: date
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: tikvgroups.core.tikv.org
spec:
  group: core.tikv.org
  scope: Namespaced
  names:
    plural: tikvgroups
    singular: tikvgroup
    kind: TiKVGroup
    listKind: TiKVGroupList
    shortNames:
    - kvg
  versions:
  - name: v1alpha1
    served: true
    storage: true
  validation:
    openAPIV3Schema:
      type: object
  additionalPrinterColumns:
  - JSONPath: .spec.cluster.name
    name: Cluster
    type: string
  - JSONPath: .spec.replicas
    name: Desired
    type: integer
  - JSONPath: .status.readyReplicas
    name: Ready
    type: integer
  - JSONPath: .status.updatedReplicas
    name: Updated
    type: integer
  - JSONPath: .status.conditions[?(@.type=="Ready")].status
    name: Ready
    type: string
  - JSONPath: .metadata.creationTimestamp
    name: Age
    type: date
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: tikvs.core.tikv.org
spec:
  group: core.tikv.org
  scope: Namespaced
  names:
    plural: tikvs
    singular: tikv
    kind: TiKV
    listKind: TiKVList
  versions:
  - name: v1alpha1
    served: true
    storage: true
  validation:
    openAPIV3Schema:
      type: object
  additionalPrinterColumns:
  - JSONPath: .spec.cluster.name
    name: Cluster
    type: string
  - JSONPath: .status.id
    name: StoreID
    type: string
  - JSONPath: .status.state
    name: StoreState
    type: string
  - JSONPath: .spec.offline
    name: Offline
    type: boolean
  - JSONPath: .status.conditions[?(@.type=="Ready")].status
    name: Ready
    type: string
  - JSONPath: .metadata.creationTimestamp
    name: Age
    type: date
